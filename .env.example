# Optional associative image generation through Cloudflare Workers AI.
# Keep the token only in .env; it is used by the local Python server and is never sent to the browser.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
